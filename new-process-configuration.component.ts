When you edit a Configuration First process (open it from the Process Configuration List and go to the SharePoint Storage Settings tab), the app should restore what you saved last time — Application in the dropdown, Site, Library, and the Folder path opened in the browser — instead of empty dropdowns. On edit, the wizard first loads the normal process details (name, scheduler, file settings, etc.); if the process type is SharePoint, it then loads the saved SharePoint location separately and passes those values into the SharePoint workspace, which connects to SharePoint, fills the dropdowns, and browses to the saved folder. While that runs, the tab shows a “Loading saved SharePoint settings…” message so you’re not looking at blank controls. That behavior lives in the new process configuration wizard (new-process-configuration → SharePoint Storage Settings tab → sharepoint-workspace), triggered when you open an existing process for edit with a configuration id in the URL.
